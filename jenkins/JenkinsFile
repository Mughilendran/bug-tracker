pipeline {
    agent any

    stages {
        stage('unit testing backend') {
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...' 
                }
            }
        }
    }
}
