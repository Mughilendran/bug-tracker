pipeline {
    agent any

    stages {
        stage('unit testing backend') {
            agent{
                docker {
                    image 'golang:1.21'
                    reuseNode true
                    }
            }
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...' 
                }
            }
        }
    }
}
